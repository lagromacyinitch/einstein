# Local secrets. public/includes/config.php is committed and reads them from .env;
# the old repo-root config.php still holds real values, so keep it out of git.
/config.php
.env

# Local editor and operating-system files.
.vscode/
.idea/
*.log
.DS_Store
Thumbs.db

# Private uploaded receipts and generated OCR cache.
public/uploads/*
!public/uploads/.htaccess
