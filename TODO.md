# TODO

- [x] Fix `academic_tutorial.html` Packages tab not showing content.

- [x] Validate HTML structure (ensure tags are correct; remove accidental invalid markup).

- [x] Ensure `switchTab()` uses matching tab ids (`tab-services` / `tab-rates`).

- [x] Confirm scripts run (no JS errors; keep `window.ENROLL_PACKAGES`).

- [ ] Quick manual test: click “Packages” and verify tab content appears.

